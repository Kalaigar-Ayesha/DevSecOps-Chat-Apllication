apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatapp.fullname" . }}-frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: {{ .Values.service.frontendPort }}
      targetPort: 8080
      protocol: TCP